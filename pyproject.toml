[project]
name = "ris-icequakes"
version = "0.1.0"
description = "A package for detecting, locationg, and visualizing icequakes"
requires-python = ">=3.12"
dependencies = [
    "notebook>=7.4.1",
    "obspy==1.4.1",
]
